<!DOCTYPE html>
<html>
<head>
    <title>Prediction Result</title>
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        header {
            width: 100%;
            background-color: #4CAF50;
            padding: 1rem;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            text-align: center;
            height: 80px;
        }
        
        .container {
            text-align: center;
            max-width: 1500px;
            padding: 20px;
        }
        
        .result-card {
            background: #fff;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 20px;
            margin: 20px;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
            width: 1000px;
        }
        
        .result-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        }
        
        h1 {
            font-size: 2.5em;
            margin-bottom: 20px;
            color: #4CAF50;
        }
        
        h2 {
            margin: 10px 0;
            color: #4CAF50;
        }
        
        h2 {
            font-size: 1.8em;
            color: #4CAF50;
        }
        
        .btn {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 20px;
            font-size: 1em;
            color: #fff;
            background-color: #73ba75;
            border-radius: 5px;
            text-decoration: none;
            transition: background-color 0.2s ease;
            width: 90%;
        }
        
        .btn:hover {
            background-color: #4CAF50;
        }
        
    </style>
</head>
<body>
    
    <section class="container">
        <h1>Prediction Result</h1>
        <div class="result-card">
            <h3>The predicted disease is:</h3>
            <h2><strong><%= predictedDisease %></strong></h2>
            <h3>Patient Should Consult:</h3>
            <h2><strong><%= predictedDoctor %></strong></h2>
            <a href="/predict" class="btn">Go Back</a>
        </div>
    </section>
</body>
</html>
